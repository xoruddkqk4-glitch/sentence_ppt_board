# English Visualization Board

영어 수업용 **영어 시각화 보드**입니다. 교사가 영어 지문을 입력하면 문장 성분을 분석·보정하고, 문장 단위 공유와 지문 전체의 추상성 단계 시각화를 하나의 흐름에서 제공합니다.

`Pretendard` 글씨체와 큰 화면 중심의 레이아웃을 사용해 교실 맨 뒤에서도 읽기 쉽게 설계했습니다. 1~7단계 이동과 TXT 저장은 모든 화면의 최상단 공통 패널에서 제공합니다.

## 현재 워크플로 (1~7단계)

1. **지문 입력**: 영어 지문을 입력하거나 TXT를 불러옵니다.
2. **어휘 분석**: 핵심 단어·표현을 최대 18개까지 등록하고 위치, 의미, Word Insights와 참고 이미지를 기록합니다. 카드 드래그앤드롭으로 순서도 바꿀 수 있습니다.
3. **어휘 공유**: 어휘를 2열×최대 9행으로 공유합니다. `0`은 전체 보기, `1`~`9`는 행의 두 카드 보기이며, 카드 클릭은 단일 포커스를 엽니다. 단일 포커스에서는 제목 오른쪽에 `현재 순번/전체 어휘 수`를 표시합니다. 첨부 이미지는 텍스트 크기에 영향을 주지 않고 카드 오른쪽 전용 영역에 표시됩니다.
4. **문장 분석**: 문장 성분을 교사가 보정합니다.
5. **문장 공유**: 문장을 한 장씩 크게 발표합니다.
6. **지문 분석**: 문장별 추상성 단계와 연결어를 지정합니다.
7. **지문 공유**: 원본·줄글식·개조식 서술을 제공합니다. 줄글식과 개조식은 `F` 키 또는 전체화면 버튼으로 교실 발표용 전체화면을 사용할 수 있습니다.

모든 단계의 수정 내용과 어휘 참고 이미지는 하나의 JSON 포함 TXT 파일에 함께 저장·복원됩니다. 저장 완료 시 어느 단계에서나 공통 알림과 저장 요약이 표시됩니다.

## 핵심 기능

- 영어 지문을 문장 단위로 자동 분리
- 핵심 어휘·표현의 위치, 의미, Word Insights, 참고 이미지 기록 및 어휘 공유 화면
- 어휘 이미지 삽입·미리보기·삭제(이미지당 최대 2MB) 및 TXT 저장/불러오기 복원
- 주어, 동사, 목적어, 보어, 제목, 소제목을 주요소(또는 이에 상응하는 주요 라인 성분)로 설정 가능
- 형용사어, 부사어를 종요소로 분석
- 주요소는 라벨 없이 색상으로 구분하고 화면 상단에 범례 표시 (제목/소제목은 발표 화면에 맞춤형 스타일로 노출)
- 종요소가 없는 순수 제목/소제목 문장은 발표 모드에서 중앙 정렬된 큰 타이틀 형식의 단독 블록으로 렌더링
- 제목/소제목 문장 내에 종요소(형용사어, 부사어)가 부여된 경우, 제목 형식을 유지하면서 하단에 종요소 연결선(부사어) 및 수식 괄호/밑줄/화살표(형용사어)가 제목 내 단어들과 이어서 매끄럽게 결합되어 표현됨
- 주요소는 한 줄에 들어오도록 글자 크기를 자동 조정
- 종요소는 한 성분이 한 줄에 표시되도록 배치
- 발표 화면 첫 단계에서는 주요소만 표시
- 클릭, 다음 버튼, 오른쪽 화살표로 종요소를 하나씩 누적 공개
- 종요소가 들어가는 위치를 문장 맨 앞, 단어 사이, 문장 맨 뒤 anchor(동그라미 점)로 표시
- 동그라미 점이 첫/마지막 단어와 겹치지 않도록 주요소 줄의 좌우 패딩을 종요소 줄보다 넓게 확보
- 여러 종요소의 점이 같은 슬롯에 겹쳐서 표시되더라도 발표 카드의 수직 나열 순서에 따라 어순 순서가 시각화되도록 설계 단순화
- 발표 화면을 전체화면으로 전환(오른쪽 위 버튼), 전체화면에서도 배경 유지로 전자칠판 판서 가능
- 종요소 앵커(점/화살표)가 항상 박스의 가로 폭 영역 내에 위치하도록 박스 Clamping 범위를 제한하고, 박스 상단에서 앵커까지 깔끔한 수직 연결선으로 연결
- 종요소 위치를 드래그 앤 드랍으로 각각 독립적으로 이동
- 긴 종요소가 화면 밖으로 나가지 않도록 동적 크기와 위치 보정
- 성분 요소(칩) 클릭 시 해당 요소만 전체 화면에 크게 보여주는 확대 화면(오버레이) 제공 (배경색은 발표 화면 테마와 완벽 동기화)
- 확대 화면 내 텍스트는 1줄에 최대 10단어씩 분할하여 왼쪽 정렬 배치
- 확대 화면 내 줄간격을 기존(2.7) 대비 2배 확장(line-height: 5.4)하여 판서 및 가시성 극대화
- 확대 화면 내 텍스트가 화면 가로폭을 최대한 꽉 채우도록 양방향(확대/축소) 동적 글자 크기 피팅(가로 120px 여백, 세로 200px 여백 및 98% 너비 기준) 적용
- 형용사어가 수식하는 원문 단어 또는 구를 선택 가능(자기 자신의 단어는 대상에서 제외)
- 형용사어는 발표 화면에서 밑줄과 화살표로 수식 관계 표시
- 형용사어 화살표는 수식 대상(주요소 포함)을 나타내는 괄호(bracket)의 밑변 정가운데를 정확히 가리킴
- 주요소와 종요소 안의 apostrophe와 구두점 보존
- 교사가 분석 결과를 드래그 앤 드랍으로 수정
- 선택한 단어만 분리하거나, 선택한 여러 단어를 하나의 묶음으로 분리
- 다른 성분 위에 겹치게 드래그하면 별도 키 없이 병합(병합 텍스트는 문장 순서로 합쳐짐)
- 분석 결과를 인덱스 포함 TXT 파일로 저장
- 저장한 TXT 파일을 다시 불러와 문장과 분석 결과 복원
- "TXT 불러오기" 및 "다른 이름으로 저장" 시 브라우저 표준 파일 피커를 연동하여 최근 작업하던 폴더 위치 기억 및 파일 핸들 갱신 기능 탑재
- "다른 이름으로 저장" 후 "현재 TXT에 저장"을 클릭하면 직전에 새로 저장한 파일에 정확히 덮어쓰기 연동
- 중요 문장 지정(2단계) 및 발표 화면 내 중요도 즉석 토글(3단계 미니 별표 ☆/★) 지원
- 중요 문장들의 순차 단축키(`1`~`9`) 매핑 및 문장 이동용 사이드바 단추 탑재
- 사이드바 내 문장 번호가 단축키 배지 유무에 무관하게 항상 수평 중심에 오도록 대칭형 정렬 구조 적용
- 중요 문서 단축키는 지문 내 등장 순서대로 이가 빠짐없이 압축 및 동적 재정렬 처리
- 2단계 수정 화면에서 1단계 대시보드로 돌아갈 때 텍스트 수정 사항이 1단계 지문에 자동으로 반영 및 취합되도록 개선
- 2단계 수정 화면에서 종요소 칩의 전구(💡) 버튼을 눌러 발표 화면(3단계)에서의 순차 공개 우선순위를 수동 지정 가능 (💡 1, 💡 2, 💡 3...)
- 중간 순위 해제 시 뒷순위 핀 번호 자동 당김(순차 압축) 처리
- 2단계에서는 원문 순서 그대로 칩을 보여주어 읽기 흐름을 방해하지 않고, 3단계 발표 모드에서만 지정된 우선순위대로 정렬하여 순차 공개
- 6단계에서 문장별 의미적 추상성 단계를 수동 지정하고 연결어 단어·구·절을 선택
- 7단계에서 전체 지문을 한 화면에 표시하고, 원본·줄글식 서술·개조식 서술 방식을 전환
- 7단계에서 숫자키 `0`으로 전체 문장을 표시하고, `1`~설정한 단계 수로 각 단계를 표시/숨김 토글
- 줄글식 서술은 항상 한 단으로, 개조식 서술은 단계별 세로 레인과 1단계 기준 파스텔 그룹 블록으로 표시
- 줄글식·개조식 서술은 전체화면(`F`)에서 상단 안내를 접고 본문에 화면 높이를 우선 배정
- 6단계에서 선택한 연결어는 7단계의 두 시각화 방식에서 노란색 형광으로 강조

## 사용 방법

1. `index.html`을 브라우저에서 엽니다.
2. **1단계-지문 입력**에서 지문을 붙여넣고 **2단계-어휘 분석 시작**을 누릅니다.
3. **2·3단계**에서 수업 핵심 어휘를 입력·공유합니다.
4. **4단계-문장 분석**에서 성분 역할·위치·분리·병합·형용사어 수식 대상을 보정합니다.
5. **5단계-문장 공유**에서 화면 클릭, 버튼, 키보드로 문장과 종요소 공개 단계를 진행합니다.
6. **6단계-지문 분석**에서 추상성 단계 개수와 각 문장의 단계를 정하고, 연결어를 선택합니다.
7. **7단계-지문 공유**에서 원본 또는 두 가지 시각화 방식으로 확인합니다.

서버나 빌드 도구 없이 동작하는 정적 웹앱입니다.

## 6단계-지문 분석

- 추상성 단계 개수는 기본 3단계이며, 2~9단계로 변경할 수 있습니다.
- 각 문장에 하나의 추상성 단계를 수동 지정합니다.
- 연결어는 단어를 클릭해 선택합니다. 구나 절은 첫 단어를 클릭한 뒤 `Shift`를 누른 채 마지막 단어를 클릭해 범위로 선택합니다.

## 7단계-지문 공유

- 첫 화면은 시각 효과가 없는 **원본** 보기입니다.
- **줄글식 서술**은 문장 순서를 유지하면서 낮은 단계일수록 더 큰 글자와 강조 색상으로 표시합니다. 3단계 기준으로 1단계는 빨강, 2단계는 파랑, 3단계는 검정입니다.
- 보기 방식과 단계 표시는 하나의 제어 패널에서 설정합니다. `0`은 전체 문장 표시, `1`~설정한 단계 수는 각 단계 문장의 표시/숨김 토글 단축키입니다.
- **개조식 서술**은 단계별 세로 레인으로 표시합니다. 같은 1단계 문장에 속한 하위 트리는 하나의 연속된 파스텔 배경 블록으로 구분됩니다.
- 개조식 카드 클릭 시 선택한 문장과 하위 문장을 단계별 레인 포커스 화면으로 볼 수 있습니다. 포커스에서 `←`/`→`는 원문 순서 이동, `↑`/`↓`는 같은 단계 문장 이동입니다.
- 개조식은 숨긴 하위 단계에 따라 글자가 커져도 상위 트리 영역을 침범하지 않도록, 각 1단계 트리에 필요한 간격만 확보합니다.
- 줄글식·개조식에서 `전체화면 (F)`을 누르면 화면을 가득 사용하며, 보기 방식·단계 필터는 유지합니다.
- 줄글식·개조식의 화면 맞춤 결과는 보기 방식·단계 필터·창 크기별로 메모리에 잠시 보관해 재전환을 빠르게 처리합니다. 전체화면 또는 보기 전환 직후에는 새 레이아웃이 확정된 뒤 다시 측정합니다.

## 발표 화면 조작

발표 화면 오른쪽 위 `전체화면 (🔍)` 버튼을 누르면 발표 화면이 전체화면으로 전환되고, 다시 누르면 전체화면을 종료합니다. 전체화면에서도 배경은 평소와 동일하게 유지되어 전자칠판 판서에 지장이 없습니다.

하단 버튼은 모든 발표 문장에 표시됩니다.

| 버튼 | 동작 |
|------|------|
| 첫 문장 | 첫 번째 문장으로 이동 |
| 이전 | 같은 문장 안에서는 이전 공개 단계로 이동, 첫 단계에서는 이전 문장으로 이동 |
| 다음 | 같은 문장 안에서는 다음 종요소 공개, 더 공개할 종요소가 없으면 다음 문장으로 이동 |
| 마지막 문장 / 이전 단계 | 마지막 문장이 아니면 마지막 문장으로 이동, 마지막 문장에서는 2단계(분석 결과 수정) 화면으로 이동 |

키보드와 클릭 조작은 다음과 같습니다.

| 입력 | 동작 |
|------|------|
| 1 ~ 9 | 중요 문장 단축키 슬롯 1~9에 해당하는 문장으로 즉시 점프 이동 |
| f 또는 F | 발표화면 전체화면 켜기/끄기 토글 |
| 화면 클릭 | 다음 버튼과 동일 |
| 오른쪽 화살표 | 다음 버튼과 동일 |
| 왼쪽 화살표 | 이전 버튼과 동일 |
| 아래 화살표 | 다음 문장으로 이동 |
| 위 화살표 | 이전 문장으로 이동 |
| Home | 첫 문장으로 이동 |
| End | 마지막 문장으로 이동 |
| Esc | 분석 결과 수정 화면으로 이동 |

## 분석 결과 수정

분석 결과 수정 화면에서는 자동 분석을 교사가 수업에 맞게 바꿀 수 있습니다.

- 성분 역할을 커스텀 드롭다운(select) 메뉴를 통해 변경 (주어~부사어는 단축키 `1`~`6` 및 번호 접두사를 탑재하고 있으며, 제목/소제목은 단축키/접두사 없이 깔끔한 텍스트로 드롭다운 메뉴 목록에 구성)
- 성분 위치를 드래그 앤 드랍으로 이동(성분 사이 빈 자리에 놓으면 순서만 변경)
- 주요소와 종요소 사이 이동
- 원하는 단어만 선택해 분리
- 여러 단어를 선택해 하나의 새 성분으로 분리
- 성분을 다른 성분 위에 겹치게 드래그해 병합(키 없이 동작, 문장 순서대로 합쳐짐)
- 형용사어가 수식하는 원문 단어를 복수 선택(Shift+클릭 연속 선택 지원, 자기 자신의 단어는 선택 대상에서 제외)
- 종요소의 발표 화면(3단계) 순차 공개 우선순위 수동 지정 및 뒷순위 당김 관리 (💡 전구 버튼)

자동 분석은 최종 정답이 아니라 교사용 초안입니다. 복잡한 문장에서는 교사가 수정하는 흐름을 전제로 합니다.

## TXT 저장과 불러오기

최상단 공통 패널의 `현재 TXT에 저장` 또는 `다른 이름으로 저장`을 누르면 현재 문장과 분석 결과가 `.txt` 파일로 저장됩니다.

저장 파일에는 다음 정보가 포함됩니다.

- 문장 번호
- 원문
- 성분 ID
- 성분 역할
- 주요소/종요소 줄 정보
- 원문 단어 인덱스
- 종요소 위치
- 형용사어 수식 대상
- 종요소 우선순위 공개 순번 (pinOrder)
- 추상성 단계 개수 (`analysisLevelCount`)
- 문장별 추상성 단계 (`abstractLevel`)
- 문장별 연결어 단어 인덱스 (`connectiveIndexes`)
- 어휘 목록 (`vocabularyItems`: 단어, 위치, 의미, Word Insights, 참고 이미지, 순서)
- 7단계 표시 방식과 단계 필터
- 앱이 복원할 수 있는 JSON 데이터

지원되는 브라우저에서는 저장 위치 선택 창이 뜹니다. 지원하지 않는 브라우저에서는 일반 다운로드 방식으로 새 파일이 생성되므로, 원본 파일을 덮어쓰지 않습니다.

`TXT 불러오기`를 누르고 저장했던 TXT 파일을 선택하면 문장, 성분, 위치, 분리/병합 결과, 형용사어 수식 대상, 추상성 단계와 연결어 선택이 복원됩니다. 4·5단계 추가 전의 저장 파일도 불러올 수 있으며, 이 경우 추상성 단계는 기본 3단계·각 문장 1단계·연결어 없음으로 시작합니다.

### 최근 폴더 기억 및 파일 세션 동기화

- **최근 작업 폴더 기억**: 브라우저의 File System Access API를 활용하여, 지문을 불러오거나 저장할 때 브라우저가 최근에 열었던 디렉토리(`id` 매핑 및 `startIn` 파일 핸들 제공)를 기억하고 다음 탐색창을 켤 때 해당 위치에서 바로 시작합니다.
- **덮어쓰기 파일 정보 즉시 갱신**: "다른 이름으로 저장"을 진행하면 현재 활성화된 파일 정보가 갱신되어, 바로 뒤에 "현재 TXT에 저장"을 눌렀을 때 이전에 저장했던 파일이 아닌, 방금 새로 저장한 파일에 정확하게 덮어쓰기가 수행됩니다.
- **1단계 자동 취합**: 2단계 문장 수정 중 "1단계-대시보드" 버튼을 눌러 이전 화면으로 돌아가더라도 수정된 텍스트가 1단계 지문에 자동으로 취합 반영되어 데이터 유실을 방지합니다.

## 화면 표시 기준

- 글씨체는 `Pretendard`를 사용합니다.
- 주요소는 밝은 색상으로 크게 표시합니다.
- 종요소는 별도 색상과 박스로 표시합니다.
- 주어, 동사, 목적어, 보어, 중요소 색상은 화면 상단 범례로 안내합니다.
- 성분 내부 단어는 줄바꿈되지 않도록 표시합니다.
- 화면이 좁거나 문장이 길면 글자 크기를 자동 조정합니다.
- 종요소는 하나의 성분이 한 줄을 차지합니다.
- 여러 종요소는 공개 순서대로 누적 표시됩니다.

## 파일 구조

```text
English Visualization Board/
├─ index.html
├─ css/
│  ├─ style.css
│  └─ vocabulary.css
├─ js/
│  ├─ main.js
│  ├─ vocabulary.js
│  └─ quick-txt-data.js
├─ docs/
│  ├─ 01-plan/
│  ├─ 02-design/
│  ├─ 03-analysis/
│  └─ 04-report/
├─ AGENTS.md
├─ DESIGN.md
└─ README.md
```

## 기술 구성

- HTML
- CSS
- JavaScript
- Pretendard 글꼴
- 브라우저 File System Access API

## 최근 워크플로 개선 사항

- 1~5단계의 두 번째 패널을 같은 카드형 디자인으로 통일했습니다. 왼쪽에는 파스텔 레드 제목 영역을, 오른쪽에는 단계별 안내와 제어 기능을 배치합니다.
- 5단계의 단계 표시 버튼은 복수 선택할 수 있으며, 숫자키 `1`~설정한 단계 수도 같은 동작을 합니다. `0`은 모든 문장을 다시 표시합니다. 숨긴 단계 문장은 원래 자리와 글자 크기를 유지한 채 보이지 않아 남은 문장이 재배치되지 않습니다.
- 단계 표시 토글은 기존 문장 요소만 즉시 숨김 처리합니다. 따라서 `줄글식 서술`과 `개조식 서술` 모두 글자 크기 계산을 다시 하지 않고 빠르게 반영됩니다.
- `개조식 서술`은 항상 1단으로 표시되며, 문장을 드래그 앤 드롭하여 순서를 바꿀 수 있습니다.
- File System Access API를 지원하는 브라우저에서는 마지막으로 불러오거나 저장한 TXT 파일 핸들을 브라우저 저장소에 기록합니다. 새로고침 후에도 다음 불러오기·다른 이름으로 저장 창이 해당 파일이 있던 폴더에서 시작합니다. 기본 파일 선택 창으로 대체되는 환경은 보안 정책상 시작 폴더를 지정할 수 없습니다.

Chrome과 Edge 계열 브라우저에서는 TXT 저장 시 저장 위치 선택 창을 사용할 수 있습니다. 다른 브라우저에서는 다운로드 방식으로 대체됩니다.

## PDCA 상태

이 프로젝트는 bkit 기준 `Starter` 레벨 프로젝트입니다.

| 항목 | 값 |
|------|----|
| 기능명 | `sentence-presentation-app` |
| 최종 phase | `completed` |
| 최종 Match Rate | 94% |
| PDCA 반복 횟수 | 1회 |
| 완료 보고서 | `docs/04-report/sentence-presentation-app.report.md` |

관련 문서는 `docs/` 폴더에 정리되어 있습니다.

## 남은 확인 사항

최종 버전에서 주요 기능은 구현되었지만, 실제 교실 화면 크기에서는 다음 항목을 수동으로 확인하는 것이 좋습니다.

- `Let's`, `today's` 같은 apostrophe 포함 문장
- 문장 맨 뒤에 붙는 긴 부사구
- 여러 종요소가 같은 anchor 근처에 붙는 문장
- 형용사어가 여러 단어 또는 종요소를 수식하는 문장
- 저장한 TXT 파일을 다시 불러오는 흐름
